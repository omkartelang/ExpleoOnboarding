server:
  port: 8085
spring:
  application:
    name: profrefinfo-service
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/TEST
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
    
