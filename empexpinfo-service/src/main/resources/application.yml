server:
  port: 8084
spring:
  application:
    name: empexpinfo-service
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/TEST
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
